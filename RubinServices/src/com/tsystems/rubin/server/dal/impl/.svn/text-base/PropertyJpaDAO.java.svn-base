package com.tsystems.rubin.server.dal.impl;

import javax.persistence.EntityManager;

import com.tsystems.rubin.server.dal.PropertyDAO;
import com.tsystems.rubin.server.dal.entities.Property;

/**
 * The Class PropertyJpaDAO.
 */
public class PropertyJpaDAO extends GenericJpaDAO<Property, Long> implements PropertyDAO {

	/**
	 * Instantiates a new property jpa dao.
	 * 
	 * @param entityManager
	 *            the entity manager
	 */
	public PropertyJpaDAO(EntityManager entityManager) {
		em = entityManager;
	}

}
