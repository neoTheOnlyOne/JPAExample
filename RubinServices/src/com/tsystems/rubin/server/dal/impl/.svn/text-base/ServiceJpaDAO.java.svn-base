package com.tsystems.rubin.server.dal.impl;

import javax.persistence.EntityManager;

import com.tsystems.rubin.server.dal.ServiceDAO;
import com.tsystems.rubin.server.dal.entities.Service;

/**
 * The Class ServiceJpaDAO.
 */
public class ServiceJpaDAO extends GenericJpaDAO<Service, Long> implements
		ServiceDAO {

	/**
	 * Instantiates a new service jpa dao.
	 * 
	 * @param entityManager
	 *            the entity manager
	 */
	public ServiceJpaDAO(EntityManager entityManager) {
		em = entityManager;
	}

}
