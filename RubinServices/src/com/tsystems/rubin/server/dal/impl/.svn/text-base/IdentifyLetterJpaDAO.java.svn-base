package com.tsystems.rubin.server.dal.impl;

import javax.persistence.EntityManager;

import com.tsystems.rubin.server.dal.IdentifyLetterDAO;
import com.tsystems.rubin.server.dal.entities.IdentifyLetter;

/**
 * The Class IdentifyLetterJpaDAO.
 */
public class IdentifyLetterJpaDAO extends GenericJpaDAO<IdentifyLetter, Long>
		implements IdentifyLetterDAO {

	/**
	 * Instantiates a new identify letter jpa dao.
	 * 
	 * @param entityManager
	 *            the entity manager
	 */
	public IdentifyLetterJpaDAO(EntityManager entityManager) {
		em = entityManager;
	}

}
