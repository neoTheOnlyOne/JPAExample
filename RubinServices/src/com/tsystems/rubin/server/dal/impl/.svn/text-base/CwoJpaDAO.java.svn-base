package com.tsystems.rubin.server.dal.impl;

import javax.persistence.EntityManager;

import com.tsystems.rubin.server.dal.CwoDAO;
import com.tsystems.rubin.server.dal.entities.CWO;
/** CwoJpaDAO **/
public class CwoJpaDAO extends GenericJpaDAO<CWO, Long> implements CwoDAO {
	/**
	 * Instantiates a new Cwo Jpa DAO.
	 * 
	 * @param entityManager
	 *            the entity manager
	 */
	public CwoJpaDAO(EntityManager entityManager) {
		em = entityManager;
	}
}
