package com.tsystems.rubin.server.dal.impl;

import javax.persistence.EntityManager;

import com.tsystems.rubin.server.dal.LruCircuitDAO;
import com.tsystems.rubin.server.dal.entities.LruCircuit;

public class LruCircuitJpaDAO extends GenericJpaDAO<LruCircuit, Long> implements
		LruCircuitDAO {

	/**
     * Instantiates a new LruCircuit jpa dao.
     * 
     * @param entityManager
     *            the entity manager
     */
    public LruCircuitJpaDAO(EntityManager entityManager) {
        em = entityManager;
    }

}
