package com.tsystems.rubin.server.dal.impl;

import javax.persistence.EntityManager;

import com.tsystems.rubin.server.dal.NetworkDAO;
import com.tsystems.rubin.server.dal.entities.Network;

/**
 * 
 * The NetworkJpaDAO class
 *
 */
public class NetworkJpaDAO extends GenericJpaDAO<Network, Long> implements NetworkDAO {

	/**
	 * Instantiates a new Network jpa dao.
	 * 
	 * @param entityManager
	 *            the entity manager
	 */
	public NetworkJpaDAO(EntityManager entityManager) {
		em = entityManager;
	}
}